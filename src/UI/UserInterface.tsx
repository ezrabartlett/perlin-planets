import React, {useRef, useEffect, RefObject} from 'react';
import { Box } from "@mui/material";

export default function UserInterface() {
    return (
      <div id="canvas-container" className="absolute bg-transparent h-200 underline">
        Test text very long
      </div>
    );
  }